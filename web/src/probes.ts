// 줄바꿈 예측을 재기 위한 측정 코퍼스.
//
// 데모 샘플 덱(sample.ts)은 불릿이 전부 한 줄이라 줄바꿈 코드를 건드리지 않는다.
// 그래서 재려면 실제로 접히는 문장이 따로 필요하다. 여기 문장들은 길이와 글자
// 구성을 흩어 놓은 것이고, 불릿 중첩 단계로 글상자 폭까지 세 가지로 벌린다
// (레벨이 깊을수록 들여쓰기만큼 폭이 좁아진다).
//
// 노리는 것: 한글 전각, 라틴 단어(중간에서 안 끊김), 숫자와 기호, 공백 비중,
// 상자보다 긴 한 단어. 어림값 세 개(전각 1.0, 라틴 0.5, 공백 0.28)로는
// 이 중 어디서 틀리는지가 안 보이므로 섞어서 넓게 깐다.

const PROBES: string[] = [
  // 순한글. 길이를 촘촘히 늘려 줄이 넘어가는 경계를 밟는다
  "상반기 실적은 목표를 웃돌았다",
  "상반기 실적은 목표를 웃돌았고 하반기 전망도 밝은 편이다",
  "상반기 실적은 목표를 웃돌았고 하반기 전망도 밝은 편이라 투자 계획을 앞당긴다",
  "엔터프라이즈 세그먼트가 상반기 성장을 견인했고 파트너 채널이 그 뒤를 이었으며 마켓플레이스는 완만하게 늘었다",
  "구독 매출 비중이 꾸준히 올라 전체의 절반을 넘어섰고 이에 따라 매출 예측 가능성이 크게 개선되어 분기 계획 수립이 수월해졌다",
  "고객 이탈률은 지난 분기 대비 소폭 낮아졌으나 대형 고객 두 곳의 계약 갱신 시점이 하반기에 몰려 있어 방심하기는 이르고 갱신 협상을 앞당겨 진행할 필요가 있다",

  // 한글에 숫자와 기호가 섞인 경우. 라틴 폭 어림이 숫자에서 어떻게 빗나가는지
  "매출 63억, 전년 대비 18% 성장",
  "신규 고객 240곳 확보, 목표 대비 131% 달성",
  "이탈률 5.1%로 0.6%p 개선, NPS 48점으로 6점 상승",
  "1분기 매출 28.4억, 2분기 51.2억, 3분기 전망 58.0억, 4분기 전망 63.5억으로 분기마다 두 자릿수 성장률을 유지",

  // 라틴이 섞인 기술 문장. 라틴 덩이는 중간에서 안 끊긴다
  "API 응답 시간을 p95 기준 320ms 에서 140ms 로 줄였다",
  "LangGraph 기반 오케스트레이션으로 재작성하면서 checkpoint 복원 경로를 단순화했다",
  "Kubernetes 클러스터를 multi-region 으로 확장하고 observability 스택을 OpenTelemetry 로 통일해 장애 추적 시간을 절반으로 줄였다",
  "PostgreSQL connection pool 설정을 조정하고 N+1 쿼리를 제거해 대시보드 초기 로딩을 2.4초에서 0.8초로 단축했다",

  // 순 라틴. 공백 비중이 높아 어림이 가장 크게 갈리는 구간
  "Revenue grew eighteen percent year over year",
  "The enterprise segment drove most of the growth in the first half of the year",
  "We reduced median latency by more than half while doubling the number of concurrent sessions the service can handle without additional hardware",

  // 상자보다 긴 한 단어. 쪼개지 않고 흘려보내는 경로
  "supercalifragilisticexpialidocious",
  "internationalization-and-localization-configuration-management",

  // 문장부호와 괄호가 많은 경우
  "핵심 지표(매출·고객·이탈률·NPS) 네 가지 모두 목표를 넘었다",
  '"보이는 대로 나온다"는 전제는, 미리보기와 파일이 같은 좌표를 쓸 때만 성립한다.',
  // 화살표와 말줄임표는 전각도 아니고 라틴도 아니라, 어림값 세 개 중 어디에도 안 맞는다
  "체크리스트: (1) 폰트 고정 → (2) 폭 실측 → (3) 어긋남 측정… 순서를 바꾸면 앞의 둘이 무의미해진다",

  // 공백이 유난히 많은 경우. 줄 맨 앞 공백 버리기 경로
  "여백   이   많은   문장   은   줄   바꿈   에   서   공백   처리   가   드러난다",
];

/** 측정용 마크다운. 불릿 세 단계와 문단으로 같은 문장을 서로 다른 폭에 태운다. */
export const PROBE_MARKDOWN = [
  "# 줄바꿈 측정",
  "",
  ...PROBES.flatMap((p) => [`- ${p}`, `  - ${p}`, `    - ${p}`, "", p, ""]),
].join("\n");

export { PROBES };
